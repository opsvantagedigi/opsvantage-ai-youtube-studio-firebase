
# Disabled: backend workflow retained for history but with no triggers
name: disabled-backend

on: []

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Noop
        run: echo "backend workflow disabled"
 

