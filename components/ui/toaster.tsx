"use client"

import React from "react"

export function Toaster() {
  // Minimal placeholder so builds succeed until a proper UI toaster exists
  return <div aria-hidden className="sr-only">Toaster</div>
}

export default Toaster
